## Plánované videá + čo treba zmeniť

---

### Video 1 — Progression (4 hry, rovnaký seed)

| Hra | Checkpoint | Súperi |
|---|---|---|
| A | `iter_0010.pt` | clever + smart + stupid |
| B | `iter_0030.pt` | clever + smart + stupid |
| C | `iter_0060.pt` | clever + smart + stupid |
| D | `iter_0100.pt` | clever + smart + stupid |

Spustenie (po úprave skriptov):
```bash
PYTHONPATH=. python play_local.py --mode mcts-vs-bots --checkpoint checkpoints/iter_0010.pt --seed 42
PYTHONPATH=. python play_local.py --mode mcts-vs-bots --checkpoint checkpoints/iter_0030.pt --seed 42
PYTHONPATH=. python play_local.py --mode mcts-vs-bots --checkpoint checkpoints/iter_0060.pt --seed 42
PYTHONPATH=. python play_local.py --mode mcts-vs-bots --checkpoint checkpoints/iter_0100.pt --seed 42
```

---

### Video 2 — Rebríček sily + Boss fight (1 hra)

| Slot | Agent |
|---|---|
| 0 | `stupid_bot` |
| 1 | `smart_bot` |
| 2 | `submission.py` (tvoj MCTS) |
| 3 | `Goose Loose` |

Spustenie — **žiadna zmena skriptov nie je potrebná**, funguje už teraz:
```bash
PYTHONPATH=. python play_local.py --mode custom --agents stupid smart mcts goose
```

---

### Video 3 — MCTS vs čistá NN (1 hra)

| Slot | Agent |
|---|---|
| 0 | `submission.py` (MCTS) |
| 1–3 | `nn_agent` × 3 (ten istý model, bez stromu) |

Spustenie — **žiadna zmena nie je potrebná**:
```bash
PYTHONPATH=. python play_local.py --mode custom --agents mcts nn nn nn
```

---

### Video 4 — Zrkadlový zápas (1 hra)

| Slot | Agent |
|---|---|
| 0–3 | `submission.py` × 4 |

Spustenie — **žiadna zmena nie je potrebná**:
```bash
PYTHONPATH=. python play_local.py --mode mcts-vs-mcts
```

---

## Potrebné zmeny v skriptoch

Zmeny sú potrebné **iba pre Video 1** (výber checkpointu). Videá 2, 3, 4 fungujú už teraz.

---

### `submission.py` — 1 zmena zostáva

`import os` už máš. Zostáva zmeniť **riadok 24**:

```python
# PRED (riadok 24):
CHECKPOINT_PATH = "checkpoints/latest.pt"

# PO:
CHECKPOINT_PATH = os.environ.get("GEESE_CHECKPOINT", "checkpoints/latest.pt")
```

---

### `play_local.py` — 3 zmeny

**1. Riadok 1** — pridať `import os`:
```python
import os
import argparse
...
```

**2. Do `parse_args()`** — pridať nový argument (pred `return parser.parse_args()`):
```python
parser.add_argument(
    "--checkpoint",
    default=None,
    help="Checkpoint file to load instead of checkpoints/latest.pt.",
)
```

**3. Do `main()`** — pridať hneď za `args = parse_args()`:
```python
if args.checkpoint:
    checkpoint_abs = str(Path(args.checkpoint).resolve())
    os.environ["GEESE_CHECKPOINT"] = checkpoint_abs
    global CHECKPOINT_PATH
    CHECKPOINT_PATH = checkpoint_abs
```

---

### Zhrnutie

| Video | Zmena skriptov potrebná |
|---|---|
| Video 1 (progression) | Áno — `submission.py` riadok 24, `play_local.py` 3 miesta |
| Video 2 (rebríček) | Nie |
| Video 3 (MCTS vs NN) | Nie |
| Video 4 (zrkadlo) | Nie |